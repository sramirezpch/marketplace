version: '3.3'

services:
  node:
    container_name: hardhat-node
    image: hardhat-node
    ports: 
      - 31337:31337
    networks:
      - network
  client:
    container_name: client
    image: marketplace-client
    # env_file:
    #   - ./marketplace/.env
    ports:
      - 3000:3000
    networks:
      - network

networks:
  network: {}